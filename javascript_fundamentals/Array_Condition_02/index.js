// what are you doing now
var numbers = [29, 84, 25, 48, 69, 88]

// 1.Get element value by index
var element = numbers[1];
console.log(element);


//2. Set element value by index

numbers[2] =77;
numbers[0] =31;
console.log(numbers);

// 3. find index of an element

console.log(numbers.indexOf(88));
console.log(numbers.indexOf(31));

var position = numbers.indexOf(77);
console.log(position);
