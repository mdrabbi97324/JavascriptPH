
let heights = [
    { name: "Dada", height: 144, relation: 'GrandFather', qualification: { level: 'H.S.C', grade: 'A' } },
    { name: "Baba", height: 132, relation: 'Father', qualification: { level: 'S.S.C', grade: 'A+' } },
    { name: "Son", height: 96, relation: 'Son', qualification: { level: 'BSc', grade: 3.8 } }
  ];
  
/*   function formatPersonInfo(person) {
    const heightInFeet = person.height / 12;
    const qualificationInfo = `Qualification is : ${person.qualification.level}, ${person.qualification.grade}`;
    return `${person.name}'s height is : ${heightInFeet} Feet, ${qualificationInfo}`;
  }
  
  function formatHeightsWithQualification(heightsArray) {
    let formattedArray = [];
  
    for (let i = 0; i < heightsArray.length; i++) {
      const formattedString = formatPersonInfo(heightsArray[i]);
      formattedArray.push(formattedString);
    }
  
    return formattedArray;
  }
  
  const formattedInfo = formatHeightsWithQualification(heights);
  console.log(formattedInfo); */

  